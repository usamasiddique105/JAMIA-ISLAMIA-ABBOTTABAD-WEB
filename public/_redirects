# Cloudflare Pages SPA Routing Fallback
# Static files in the build output (sitemap.xml, robots.txt, assets, etc.) are served directly by Cloudflare Pages CDN before this fallback.
/*    /index.html   200
